/**
 * ============================================================================
 * Sincronização do CHANGELOG
 * ============================================================================
 *
 * Este módulo será responsável futuramente por preparar
 * automaticamente o CHANGELOG seguindo o padrão
 * Keep a Changelog.
 *
 * Nesta primeira implementação ainda não altera o arquivo.
 * Apenas registra que a etapa foi executada.
 * ============================================================================
 */

export function updateChangelog(version) {

  console.log(
    `📝 CHANGELOG preparado para a versão ${version}.`
  );

}
